TARGET = savingSystem
CC = gcc

PREF_SRC = ./
PREF_OBJ = ./

SRC = $(wildcard $(PREF_SRC)*.c)
OBJ = $(patsubst $(PREF_SRC)%.c, $(PREF_OBJ)%.o, $(SRC))

${TARGET}: ${OBJ}
	ar r ${TARGET}.a ${OBJ}

${PREF_OBJ}%.o: ${PREF_SRC}%.c
	${CC} -c $< -o $@

clean:
	rm ${TARGET} ${OBJ}
